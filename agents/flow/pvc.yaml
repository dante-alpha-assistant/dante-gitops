apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flow-data
  namespace: agents
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
