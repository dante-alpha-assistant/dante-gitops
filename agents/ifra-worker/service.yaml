apiVersion: v1
kind: Service
metadata:
  name: ifra-worker
  namespace: agents
  labels:
    app: ifra-worker
spec:
  selector:
    app: ifra-worker
  ports:
    - port: 18789
      targetPort: 18789
      protocol: TCP
