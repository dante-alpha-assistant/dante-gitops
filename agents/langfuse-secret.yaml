apiVersion: v1
kind: Secret
metadata:
  name: langfuse-env
  namespace: agents
  labels:
    app: langfuse
    purpose: observability
type: Opaque
stringData:
  LANGFUSE_PUBLIC_KEY: "REPLACE_ME"
  LANGFUSE_SECRET_KEY: "REPLACE_ME"
