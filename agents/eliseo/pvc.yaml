apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: eliseo-data
  namespace: agents
  labels:
    app: eliseo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
