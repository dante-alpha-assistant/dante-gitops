apiVersion: v1
kind: Service
metadata:
  name: setup-agent
  namespace: agents
  labels:
    app: setup-agent
spec:
  selector:
    app: setup-agent
  ports:
    - port: 18789
      targetPort: 18789
      protocol: TCP
