apiVersion: apps/v1
kind: Deployment
metadata:
  name: mu
  namespace: agents
  labels:
    app: mu
    role: agent
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: mu
  template:
    metadata:
      labels:
        app: mu
        role: agent
    spec:
      initContainers:
        - name: init-config
          image: node:22-bookworm-slim
          command: ["node", "-e"]
          args:
            - |
              const fs = require('fs');
              const e = process.env;
              const config = {
                gateway: {
                  port: 18789,
                  mode: "local",
                  bind: "lan",
                  auth: { mode: "token", token: e.OPENCLAW_GATEWAY_TOKEN || "" },
                  tools: { allow: ["sessions_spawn","sessions_send","sessions_list","sessions_history","session_status","cron"] },
                  controlUi: { dangerouslyAllowHostHeaderOriginFallback: true }
                },
                channels: {
                  discord: {
                    enabled: true,
                    token: e.DISCORD_TOKEN || "",
                    groupPolicy: "open",
                    dmPolicy: "pairing",
                    allowBots: true
                  }
                },
                models: {
                  providers: {
                    anthropic: {
                      baseUrl: "https://api.anthropic.com",
                      apiKey: e.ANTHROPIC_API_KEY || "",
                      models: [{ id: "moonshotai/kimi-k2.5", name: "moonshotai/kimi-k2.5", api: "anthropic-messages", reasoning: true, input: ["text","image"], cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0 }, contextWindow: 200000, maxTokens: 16384 }]
                    },
                    openrouter: {
                      baseUrl: "https://openrouter.ai/api/v1",
                      apiKey: e.OPENROUTER_API_KEY || "",
                      models: [
                        { id: "google/gemini-3-pro-preview", name: "gemini-3-pro-preview", api: "openai-completions", reasoning: false, input: ["text","image"], cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0 }, contextWindow: 1048576, maxTokens: 8192 },
                        { id: "moonshotai/kimi-k2.5", name: "kimi-k2.5", api: "openai-completions", reasoning: true, input: ["text","image"], cost: { input: 0.45, output: 2.25, cacheRead: 0.07, cacheWrite: 0 }, contextWindow: 262144, maxTokens: 8192 }
                      ]
                    },
                    openai: {
                      baseUrl: "https://api.openai.com/v1",
                      apiKey: e.OPENAI_API_KEY || "",
                      auth: "api-key",
                      api: "openai-responses",
                      models: [
                        { id: "gpt-5.2", name: "gpt-5.2", api: "openai-responses", reasoning: true, input: ["text","image"], cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0 }, contextWindow: 400000, maxTokens: 8192 },
                        { id: "gpt-5.2-codex", name: "gpt-5.2-codex", api: "openai-responses", reasoning: true, input: ["text"], cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0 }, contextWindow: 400000, maxTokens: 16384 }
                      ]
                    }
                  }
                },
                agents: {
                  defaults: {
                    model: { primary: "openrouter/moonshotai/kimi-k2.5" },
                    workspace: "/root/.openclaw/workspace",
                    compaction: { mode: "safeguard" },
                    maxConcurrent: 4,
                    subagents: { maxConcurrent: 8 }
                  }
                },
                hooks: { enabled: true, token: "mu-hooks-tok-2026", allowRequestSessionKey: true, defaultSessionKey: "hook:default", allowedSessionKeyPrefixes: ["hook:"] },
                tools: { sessions: { visibility: "all" } }
              };
              fs.mkdirSync('/root/.openclaw/workspace', { recursive: true });
              fs.mkdirSync('/root/.openclaw/skills', { recursive: true });
              // --- Langfuse Cloud OTLP tracing ---
              if (e.LANGFUSE_PUBLIC_KEY && e.LANGFUSE_SECRET_KEY && !e.LANGFUSE_PUBLIC_KEY.includes('REPLACE_ME')) {
                const lfAuth = Buffer.from(e.LANGFUSE_PUBLIC_KEY + ':' + e.LANGFUSE_SECRET_KEY).toString('base64');
                const lfBase = e.LANGFUSE_BASEURL || 'https://cloud.langfuse.com';
                config.plugins = config.plugins || { allow: [], entries: {} };
                config.plugins.allow = [...new Set([...(config.plugins.allow || []), 'diagnostics-otel'])];
                config.plugins.entries = config.plugins.entries || {};
                config.plugins.entries['diagnostics-otel'] = { enabled: true };
                config.diagnostics = {
                  enabled: true,
                  otel: {
                    enabled: true,
                    endpoint: lfBase + '/api/public/otel',
                    headers: { Authorization: 'Basic ' + lfAuth },
                    serviceName: 'openclaw-mu',
                    traces: true,
                    metrics: true,
                    logs: false,
                    sampleRate: 1.0,
                    flushIntervalMs: 30000
                  }
                };
              }
              fs.writeFileSync('/root/.openclaw/openclaw.json', JSON.stringify(config, null, 2));
              console.log('Config written OK');
          envFrom:
            - secretRef:
                name: mu-env
            - secretRef:
                name: langfuse-credentials
          volumeMounts:
            - name: data
              mountPath: /root/.openclaw
      containers:
        - name: openclaw
          image: ghcr.io/dante-alpha-assistant/openclaw-agent:latest
          ports:
            - containerPort: 18789
          envFrom:
            - secretRef:
                name: mu-env
            - secretRef:
                name: langfuse-credentials
          volumeMounts:
            - name: data
              mountPath: /root/.openclaw
          resources:
            requests:
              cpu: 1000m
              memory: 1Gi
            limits:
              cpu: 4000m
              memory: 8Gi
          livenessProbe:
            tcpSocket:
              port: 18789
            initialDelaySeconds: 120
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          readinessProbe:
            tcpSocket:
              port: 18789
            initialDelaySeconds: 60
            periodSeconds: 5
            timeoutSeconds: 5
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: mu-data
      imagePullSecrets:
        - name: ghcr-secret
